
Review and refine the generated design before decomposition. The default pack
implementation runs two focused reviewer agents and a synthesize/apply loop
until the design is approved. Local packs can override this step and keep the
same sink metadata protocol. Current mode is {{mode}}.
